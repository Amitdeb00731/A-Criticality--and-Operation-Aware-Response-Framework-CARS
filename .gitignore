# ---- LaTeX build artefacts (regenerate from source) ----
*.aux
*.log
*.toc
*.out
*.lof
*.lot
*.lol
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.nav
*.snm
*.vrb
*.idx
*.ilg
*.ind
*.acn
*.acr
*.alg
*.glg
*.glo
*.gls
*.run.xml

# Built dissertation PDF (regenerate with pdflatex)

# ---- Python ----
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.venv/
venv/

# ---- Heavy artefacts (kept locally, not committed) ----
# 23 MB raw Snort journal (supporting evidence bundle; the vital slices are in the report)
report/gap_evidence/**/*.journal.txt
# Unused high-resolution figure originals (the report uses the .jpg versions)
report/figures/Factory_io_scene.png
report/figures/Teaching_box_panel.jpg
# Raw overnight capture pcaps (heavy; keep the analysed summaries, not the raw captures)
07_Evaluation/overnight/results/**/*.pcap

# ---- Editor / OS junk ----
.DS_Store
Thumbs.db
*.bak
*~
.vscode/
.idea/

# overnight run raw captures (large; keep only logs/*.csv + summaries in git)
07_Evaluation/overnight/night_*/**/pcap/
07_Evaluation/overnight/night_*/**/*.pcap

# tooling caches
.ruff_cache/
.pytest_cache/
